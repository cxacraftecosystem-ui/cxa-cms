{
  "name": "server-only",
  "version": "0.0.0-noop",
  "private": true,
  "main": "index.js",
  "description": "Inert stand-in for the server-only marker package, so `npm run seed` and `npm run smoke` resolve it under plain node. See index.js for why the real package cannot be used."
}
